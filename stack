#include <iostream>       // std::cout
#include <stack>          // std::stack
using namespace std;
int main ()
{
  stack<int> me;
  for(int i=0; i <5; ++i) ne.push(i);
  while(!me.empty())
  {
      cout << ' ' << me.top();
      me.pop();
  }
  cout <'\n';
  return 0;
}
